# 검증 결과 — 2026-09-07 (초기 스냅샷, 이후 대폭 변경됨)

> **이 문서는 2026-09-07 시점의 1회성 검증 기록입니다.** 이후 RAG 가이드라인 검사, 영문약어/정의 추천, 도메인 재배치 등 여러 기능이 추가되며 아래 세부 흐름(예: 도메인 선택→정의 순서)은 지금과 다릅니다. **현재 아키텍처와 미해결 이슈는 [../CLAUDE.md](../CLAUDE.md)를 보세요.** 이 문서는 초기 통합 시점의 역사적 기록으로만 남겨둡니다.

- `pytest -q`: **23 passed, 1 skipped**. 제외된 1건은 별도 실행을 요구하는 유료 OpenAI 비교 테스트입니다.
- 실제 PostgreSQL 및 pgvector를 이용한 검증: 명명 규칙, 검색, 도메인 분포, 중복 방지, 소유권, 최종 확인, 변경 무효화, 동시 제출, 도움말 상태 유지 통과.
- Dify 지식 문서: 12건 모두 `completed`. 실제 지식 검색 노드 성공 및 2~3건 결과 반환 확인.
- 실제 HTTP `/v1/chat-messages` 대화 확인:
  - 일일권장칼로리 추출 → 사용자 확인 → 도메인 선택 → 정의 입력.
  - 정의 입력 중 기존 정의 조회 요청을 별도 의도로 처리.
  - 기존 일일권장열량과 동일 정의 입력 → `definition_blocked`, 등록 요청 없음.
  - 식단검토사유 → 별도 정의 비교 → 최종 확인 → `submitted`.
  - BMI → 사용자 확인 → 규칙 위반 및 체질량지수/체질량지수(BMI) 수정 후보.
  - 체질량지수로 수정 → 다시 사용자 확인 → 기존 정확 일치로 등록 차단.
- 테스트 승인 대기 요청: `bff3233b-965b-4e41-80a6-cfdb22ad6588`, 식단검토사유, `PENDING_REVIEW`.
- 요청 저장 후 표준용어 수는 12건으로 유지됨. 정식 표준으로 자동 등록되지 않음.
- MCP 8100 수신 및 DB/모델 설정 health 정상 확인.

통합 중 발견한 Dify MCP `text` 빈 출력 문제는 `json` 참조로 수정했습니다. 검색 문서가 초기 확인 단계를 덮어쓰는 설명 문제는 단계별 코드 노드로 참고 자료를 제한했습니다. 업무 처리와 저장은 MCP 결과를 기준으로 합니다.

Dify 앱은 게시한 상태입니다. 검증은 로컬 HTTP와 실제 Dify 서버에서 수행했으며 모바일 브라우저/외부 임시 터널의 화면 동작 및 SSE는 이번 검증 범위가 아닙니다. 임시 터널의 기존 SSE 제약은 이 구현으로 바뀌지 않습니다.
